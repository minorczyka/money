# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~
POST    /signUp                     controllers.UserController.signUp
POST    /signIn                     controllers.UserController.signIn

POST    /room/create                controllers.RoomController.create
GET     /room/list                  controllers.RoomController.list
GET     /room/details/:id           controllers.RoomController.details(id: Int)
POST    /room/person/add/:id        controllers.RoomController.addPerson(id: Int)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
